This folder contains the files for [lokksmith.dev](https://lokksmith.dev/)
managed via [Zensical](https://zensical.org/).

[Python 3](https://python.org/) is required. Install `zensical` via a 
virtual environment (`setup_venv.sh`) or via

```
pipx install zensical --include-deps
```
